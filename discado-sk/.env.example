# .env.example
# Copier ce fichier en .env et remplir avec vos vraies valeurs
# Ne jamais commiter le fichier .env dans Git !

# Base de données
DATABASE_PATH=./database/discado.db

# Clés de cryptage (générer des clés sécurisées)
# Utilisez: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here

# Utilisez: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ENCRYPTION_IV=your-16-byte-hex-iv-here

# Clé de session (générer une clé sécurisée)
# Utilisez: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SECRET_KEY=your-secret-session-key-here

# Application
NODE_ENV=development
PUBLIC_APP_NAME=Discado
